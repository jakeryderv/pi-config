# pi-config Project Instructions

The global Pi instructions already apply. For this repository:

- Treat changes as global configuration changes that affect every Pi project.
- Preserve compatibility with the currently installed Pi version.
- Run `just check` after modifying extensions.
